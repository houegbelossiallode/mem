<form method="post" action="{{ path('app_calibre_repas_delete', {'id': calibre_repa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calibre_repa.id) }}">
    <button class="btn">Delete</button>
</form>
