{% extends 'base.html.twig' %}

{% block title %}CalibreRepas{% endblock %}

{% block body %}
    <h1>CalibreRepas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calibre_repa.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ calibre_repa.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_calibre_repas_index') }}">back to list</a>

    <a href="{{ path('app_calibre_repas_edit', {'id': calibre_repa.id}) }}">edit</a>

    {{ include('calibre_repas/_delete_form.html.twig') }}
{% endblock %}
