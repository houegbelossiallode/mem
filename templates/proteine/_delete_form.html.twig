<form method="post" action="{{ path('app_proteine_delete', {'id': proteine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proteine.id) }}">
    <button class="btn">Delete</button>
</form>
