{% extends 'template.html.twig' %}

{% block title %}Proteine{% endblock %}

{% block body %}
    <h1>Proteine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proteine.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ proteine.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_proteine_index') }}">back to list</a>

    <a href="{{ path('app_proteine_edit', {'id': proteine.id}) }}">edit</a>

    {{ include('proteine/_delete_form.html.twig') }}
{% endblock %}
