{% extends 'template.html.twig' %}

{% block title %}VenteRepas{% endblock %}

{% block body %}
    <h1>VenteRepas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vente_repa.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ vente_repa.designation }}</td>
            </tr>
            <tr>
                <th>Quantite_vendue</th>
                <td>{{ vente_repa.quantiteVendue }}</td>
            </tr>
            <tr>
                <th>Prix_vente</th>
                <td>{{ vente_repa.prixVente }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ vente_repa.montant }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ vente_repa.createdAt ? vente_repa.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vente_repas_index') }}">back to list</a>

    <a href="{{ path('app_vente_repas_edit', {'id': vente_repa.id}) }}">edit</a>

    {{ include('vente_repas/_delete_form.html.twig') }}
{% endblock %}
