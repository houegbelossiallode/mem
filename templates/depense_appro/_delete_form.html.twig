<form method="post" action="{{ path('app_depense_appro_delete', {'id': depense_appro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depense_appro.id) }}">
    <button class="btn">Delete</button>
</form>
