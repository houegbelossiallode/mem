<form method="post" action="{{ path('app_recette_delete', {'vente_boisson': recette.vente_boisson}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recette.vente_boisson) }}">
    <button class="btn">Delete</button>
</form>
