<form method="post" action="{{ path('app_vivre_delete', {'id': vivre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vivre.id) }}">
    <button class="btn">Delete</button>
</form>
