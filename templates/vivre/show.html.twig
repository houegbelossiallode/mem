{% extends 'template.html.twig' %}

{% block title %}Vivre{% endblock %}

{% block body %}
    <h1>Vivre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vivre.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ vivre.designation }}</td>
            </tr>
            <tr>
                <th>Qte_stock</th>
                <td>{{ vivre.qteStock }}</td>
            </tr>
            <tr>
                <th>Qte_sortir</th>
                <td>{{ vivre.qteSortir }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vivre_index') }}">back to list</a>

    <a href="{{ path('app_vivre_edit', {'id': vivre.id}) }}">edit</a>

    {{ include('vivre/_delete_form.html.twig') }}
{% endblock %}
