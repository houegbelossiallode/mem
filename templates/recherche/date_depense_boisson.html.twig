{% extends 'template.html.twig' %}

{% block title %}Hello RechercheController!{% endblock %}

{% block body %}

<section class="section">
      <div class="row">
        <div class="col-lg-12">

          <div class="card">
            <div class="card-body">
              <h5 class="card-title"></h5>


{{ form_start(form) }}
<div class="row">

<div class="col-2">{{ form_row(form.date) }}</div>

</div>

{{ form_end(form) }}



<div class="col-10">


{% if app.request.method == 'POST' %}
<form class="form-horizontal">
      <div class="table-responsive">
        <table class="table table-bordered table-striped table-highlight">
                          <thead>
                              <tr>
                                  <th>Boisson</th>
                                  
                                  <th>Nb casier acheté</th>
                                  <th>Prix du Casier</th>
                                  <th>Montant</th>
                                  <th>Nombre de trou</th>
                              </tr>
                          </thead>
                          
                              
                         
                          <tbody>
                              {% if donnees |length == 0 %}
                          <h4 class="text text-primary">Aucun resultats pour la recherche</h4>
                              {% else %}
                              <h4 class="text text-primary">Résultats de la recherche</h4>
                              {% for donnee in donnees %}
                              
                                  <tr>
                                      
                                      <td>{{ donnee.designation}}</td>
                                      <td>{{ donnee.quantite}}</td>
                                      <td>{{ donnee.prix}}</td>
                                      <td>{{ donnee.total}}</td>
                                      <td>{{ donnee.trou }}</td>
                                  </tr>
                                  {% endfor %}
                                  
                                  
                              {% endif %}
                          </tbody>
                      
                      </table>
                 
              </div>
          </form>
          {% endif %}
</div>

{% endblock %}
