{% extends 'base.html.twig' %}

{% block title %}Calibre{% endblock %}

{% block body %}
    <h1>Calibre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calibre.id }}</td>
            </tr>
            <tr>
                <th>Masse</th>
                <td>{{ calibre.masse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_calibre_index') }}">back to list</a>

    <a href="{{ path('app_calibre_edit', {'id': calibre.id}) }}">edit</a>

    {{ include('calibre/_delete_form.html.twig') }}
{% endblock %}
